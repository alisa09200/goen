<!DOCTYPE html>
<html>
  <head>
    <title>Goen</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><%=link_to root_path, data: {"turbolinks" => false}  do %><%= image_tag "logo5.png" %><% end %></h1>
        <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <span><%= current_user.nickname %>
              <ul class="user__info">
                <li>
                  <%= link_to "先生専用　　ページ", "/users/#{current_user.id}", data: { confirm: "【かくにん】あなたは ほんとうに せんせい ですか？" } %>
                  <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
                </li>
              </ul>
            </span>
            <%= link_to "先生動画投稿", new_tweet_path, class: "post", data: { confirm: "【かくにん】あなたは ほんとうに せんせい ですか？" } %>
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "Log in", new_user_session_path, class: "post" %>
            <%= link_to "Sign up", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
      </p>
    </footer>
  </body>
</html>